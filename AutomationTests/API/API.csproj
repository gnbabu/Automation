<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- NUnit deliberately pinned older than the shared central version (see
         Directory.Packages.props) - this is a superseded, unrelated prototype, not part
         of the real Selenium framework. -->
    <PackageReference Include="NUnit" VersionOverride="3.14.0" />
    <PackageReference Include="Swashbuckle.AspNetCore" />
  </ItemGroup>

</Project>
